= simple_form_for @doctor do |f|

  = f.error_notification

  = f.input :name
  = f.input :specialization

  = f.association :hospital, prompt: "Select Hospital"

  = f.simple_fields_for :doctor_profile do |profile|
    = profile.input :experience
    = profile.input :qualification

  = f.button :submit