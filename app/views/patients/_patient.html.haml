%tr{ id: dom_id(patient) }
  %td= patient.name
  %td= patient.age 
  %td= patient.gender
  %td= patient.phone
  %td= link_to "Show", patient_path(patient)
  %td= link_to "Edit", edit_patient_path(patient),data: { turbo_frame: "modal" }
  %td= link_to "Delete", patient_path(patient), data: {turbo_method: :delete}
