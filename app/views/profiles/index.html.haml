- if notice
  %p{style: "color: red;"}
  = notice

%h1 Profiles
= link_to "New Profile", new_profile_path

%table{border: "1"}
  %thead
    %tr
      
      %th Doctor
      %th Consultation fee
      %th Experience
      %th{ colspan: "3"} Actions

  %tbody
    - @profiles.each do |profile|
      %tr
        %td= profile.doctor.name
        %td= profile.consultation_fee
        %td= profile.experience
        %td= link_to "Show", profile_path(profile)
        %td= link_to "Edit", edit_profile_path(profile)
        %td= link_to "Delete", profile_path(profile), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
